## 0.3.2：文件夹行数合计与批量审阅

继续使用原来的 **Changes Between…** 文件树，不迁移到 Project View，也不改变红绿配色。

### 新功能

- 文件夹、模块及目录分组显示所有下级变更文件的 `+新增 / -删除` 合计，包括折叠子目录、删除文本文件和压缩显示的包路径。
- 文件夹显示子文件审阅进度；全部可审阅文件完成后显示“已审阅”，内容变化时显示剩余进度和“需重审”。
- 选中文件夹，点击原生工具栏“标记已审阅”，批量标记其中全部可审阅变更文件；再次点击“取消已审阅”可整体取消。
- 支持多个文件夹、多层父子目录与文件混合选择，重叠选择自动去重。只处理当前比较树中的文件，不扫描磁盘上其他文件。
- “审阅并下一个”支持文件夹和多选，完成后从所选范围的末尾继续查找。

### 安全边界

- 目录批量标记跳过已知二进制、读取失败及内容超限文件，显示“跳过 N”；尚在核对的子文件不会被当作已完成，也不会被静默跳过，需等待内容指纹就绪。
- 部分文件不能统计时显示“部分统计”，没有有效统计时不伪装成 `+0 -0`。
- 文件夹没有独立的永久“已审阅”标记：状态由当前子文件推导。新增或改变的子文件不会继承文件夹的完成状态。
- Diff 工具栏仍只标记它实际显示的那个文件，不受文件树选中文件夹的影响。直接多选文件仍保留完整校验、全部成功后才写入的行为。
- 合计从已有后台统计缓存生成；renderer 不遍历磁盘、不读取 revision。加总使用 long，避免目录合计 int 溢出。

### 安装

下载 **ChangeLines-0.3.2.zip**，不解压：Settings → Plugins → 齿轮 → Install Plugin from Disk… → 重启。

最低 **IDEA 2026.1（261）**。构建包含新增的原生文件夹树、批量操作、混合不可审阅项、等待中、内容变更、导航和 Diff 隔离测试，并保留已有回归测试及 2026.1 / 2026.2.0.1 兼容性检查。发布附件来自通过全部检查的同次构建，附 SHA256SUMS。

这版通过现有原生工具栏操作文件夹，不新增行尾独立按钮。自动化平台测试不等同于 macOS 实机人工外观验收。
