## 0.3.3：侧边栏过窄时固定显示变更统计

继续增强原来的 **Changes Between / Local Changes 文件树**，不改行数算法、审阅记录或红绿配色。

### 显示规则

- 空间足够：`+新增 / -删除` 和审阅信息继续紧跟原生文件名／路径，不统一强制右对齐。
- 超出右侧可见范围：统计区域固定到侧边栏右侧，前面的长路径／文件名显示省略号，不与统计文字重叠。
- 文件、文件夹合计、展开／折叠的分组使用同一规则。
- 拖宽侧边栏后自动恢复正常行内显示；水平滚动时基于当前 viewport 定位，而不是整棵树的内容宽度。
- 保留原生复选框、图标、选中背景、文件背景以及原有红绿配色，不加设置项。
- 特别窄时先省略尾部审阅说明；完整行文本仍可从 tooltip／无障碍名称读取。如果窄到复选框、图标和数字本身都放不下，仍需要适当拓宽，不将数值改为缩写或伪造零行。

### 实现与验证

仅在绘制时调整文字和统计位置，不替换 tree model，不修改审阅数据，不在绘制中读取文件／revision，也不在 renderer 里递归查询 row bounds。绘制后恢复原生文本片段、属性和链接标记，保留原始行宽供原生水平滚动和展开提示使用。

增加 CellRendererPane 像素测试和真实 ChangesTree 文件／文件夹集成测试，覆盖短行、长路径、选中背景、缩放宽度、深层缩进、水平滚动、窄栏审阅说明、Unicode 截断、完整文本恢复及 viewport listener 清理。测试生成的图片随 CI verification-reports 上传。

最低 **IDEA 2026.1（261）**；保留 IDEA 2026.1 / 2026.2.0.1 兼容性检查与安装包检查。自动化测试不等同于 macOS 实机人工验收。

### 安装

发布后下载 **ChangeLines-0.3.3.zip**，不解压：Settings → Plugins → 齿轮 → Install Plugin from Disk… → 重启。
